Perform a full audit of the almaasa-store codebase. Check for:

1. **TypeScript errors**: Run `cd /home/user/almaasa-store && npx tsc --noEmit 2>&1 | head -50`
2. **Build errors**: Run `cd /home/user/almaasa-store && npm run build 2>&1 | tail -30`
3. **Broken imports**: Search for imports that reference non-existent files
4. **Missing routes**: Check src/App.tsx for all routes and verify each page component exists
5. **Console errors**: Check for any obvious runtime issues in components (undefined access, missing props)
6. **Unused imports**: Check for imported items that are never used
7. **Dead code**: Components imported in App.tsx but never rendered

Report all findings in Arabic with file paths and line numbers. Prioritize: 🔴 Critical errors → 🟡 Warnings → 🔵 Suggestions.
