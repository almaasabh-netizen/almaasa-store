Read src/data.ts to understand all localStorage keys used by the store.

Generate a backup summary report in Arabic that lists:
1. All localStorage keys the store uses and what data they contain
2. How to manually export the data (copy from browser DevTools > Application > localStorage)
3. A checklist of all important data to back up: products, orders, categories, coupons, settings, shipping zones, hero banners, reviews

Also check if there are any data integrity issues: orphaned references, missing required fields in products, orders without customer info, etc.
